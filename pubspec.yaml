name: desi_ai_app
description: "A new Flutter project for AI Agent & Studio"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.4
  shared_preferences: ^2.2.3
  http: ^1.2.0
  image_picker: ^1.0.7  # <--- यह नया पैकेज यहाँ जोड़ दिया है!

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  webview_flutter: ^4.4.4
  
flutter:
  uses-material-design: true
