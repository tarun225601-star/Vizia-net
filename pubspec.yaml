name: margtasni
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  video_player: ^2.8.2
  audioplayers: ^6.0.0    # <-- ये नया जोड़ दिया है भाई, अब गाने बजेंगे!

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
